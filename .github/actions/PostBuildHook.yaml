name: Post-Build Hook

on:
  push:
    branches:
      - '*'

jobs:
  postBuildHook:
    runs-on: ubuntu-latest

    steps:
      - name: Post-Build Hook
        run: |
          echo "Running postBuildHook.sh"