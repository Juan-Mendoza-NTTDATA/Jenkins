name: MegaLinter
on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: MegaLinter
        run: |
          container megalinter {
            /entrypoint.sh
          }