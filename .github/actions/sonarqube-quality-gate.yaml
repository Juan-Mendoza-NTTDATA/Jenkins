name: SonarQube Quality Gate

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Run SonarQube Quality Gate
        run: |
          # Add the commands to run the SonarQube Quality Gate here