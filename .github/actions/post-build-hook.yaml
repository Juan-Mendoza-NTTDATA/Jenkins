name: Post-Build Hook

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Run Post-Build Hook
        run: |
          # Add the commands to run the post-build hook here