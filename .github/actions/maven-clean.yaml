name: Maven Clean

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Run Maven Clean
        run: |
          # Add the commands to run the maven clean here