name: GHCR Login

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Run GHCR Login
        run: |
          # Add the commands to run the GHCR login here