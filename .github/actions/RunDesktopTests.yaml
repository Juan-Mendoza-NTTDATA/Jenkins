name: Run Desktop Tests

on:
  push:
    branches:
      - '*'

jobs:
  runDesktopTests:
    runs-on: ubuntu-latest

    steps:
      - name: Run Desktop Tests
        run: |
          echo "Running ./mvnw test -DselenoidUrl='$SELENIUM_HUB_URL' -Dgroups=com.mydomain.category.interfaces.DesktopTests -DthreadCount='$THREAD_COUNT'"