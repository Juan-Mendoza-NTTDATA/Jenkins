name: Run Tests in Parallel

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Run Tests in Parallel
        run: |
          # Add the commands to run the tests in parallel here